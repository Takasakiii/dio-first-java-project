server:
  port: 2000

spring:
  datasource:
    url: 'jdbc:h2:file:~/fistProject'
    username: 'sa'
    password: ''
    driver-class-name: 'org.h2.Driver'
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: update
  h2:
    console:
      enabled: true
      path: /h2